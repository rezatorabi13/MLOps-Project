# Files NOT to copy into the image (keeps it small & avoids leaking junk).
# The container loads the model from serving_model/, so it does NOT need the
# experiment database or the raw mlruns artifacts.
.venv/
mlruns/
mlflow.db
__pycache__/
*.pyc
.git/
.gitignore
*.md
