# Pinned dependencies for a reproducible container.
# Pinning exact versions means the image builds the SAME way every time,
# on every machine — no "works on my laptop" surprises.
mlflow==3.13.0
scikit-learn==1.9.0
pandas==2.3.3
fastapi==0.136.3
uvicorn==0.49.0
prometheus-fastapi-instrumentator==8.0.0
kafka-python==2.3.2
